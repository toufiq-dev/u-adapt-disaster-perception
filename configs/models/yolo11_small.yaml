# YOLO11-small — fallback backbone / efficient Colab alternative (ultralytics, AGPL-3.0)
# NOTE: AGPL-3.0 license — verify implications for the thesis (docs/licenses.md, issue #1)
name: yolo11_small
framework: ultralytics
checkpoint: yolo11s.pt
license: AGPL-3.0
inference:
  conf: 0.05
  device: cuda
  top_k: 100
feature_source: detector_internal
